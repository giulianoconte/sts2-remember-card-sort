commit*
*.zip

# Captured Godot session logs (from redeploy.sh / watch-log.sh) and the
# watcher's PID/output files.
logs/

# Steam Workshop staging (generated by workshop-stage; configs are tracked)
workshop/content/
workshop/mod-uploader.log
